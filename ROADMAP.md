# خارطة طريق المشروع — منصة EduAI

> وثيقة تقسيم العمل بين أعضاء الفريق (3 مطورين). حُدّثت بتاريخ 2026-09-07.
> تهدف إلى توزيع واضح غير متداخل بين ثلاثة مسارات، مع سير عمل Git موحّد.

---

## 1. نبذة عن المشروع

منصة تعليمية ذكية عربية (مشروع تخرج):
- **الخلف**: FastAPI + SQLite — `backend/`
- **الأمام**: React 19 + Vite + Tailwind v4 — `frontend/`
- **الرندر**: محرك عروض `presentation_engine` (HTML → Chrome headless → PPTX/PDF/PNG)

### الميزات الحالية
| المجموعة | الميزات |
|---|---|
| المستندات | رفع/FastAPI متعدد الصيغ، فهرسة + RAG (TF-IDF)، تقسيم لفقرات |
| أدوات AI | محادثة استشهادية (Streaming)، تلخيص + خريطة ذهنية، اختبارات MCQ + بطاقات، تدقيق نحوي، ترجمة أكاديمية (3 أوضاع)، مولّد عروض Deck Studio |
| الهويات البصرية | معرض قوالب، توليد AI، استيراد من PPTX، قوالب مخصصة كائنات CSS |
| الإدارة | لوحة أدمن كاملة (مستخدمون/إعدادات/سجلات/برومبتات)، بنك برومبتات، مزودات متعددة |
| الحسابات | بريد+كلمة مرور، Google OAuth، دخول طالب سريع، أدوار (student/admin) |

---

## 2. الفجوات والأولويات (نتيجة المسح)

| # | المجال | الوصف | الأولوية |
|---|---|---|---|
| G1 | الأمان | هوية المستخدم عبر `X-User-Id` غير موقّعة (قابل للتزوير) | حرجة |
| G2 | الأمان | تجاوز فحص Google JWT بلا تحقق التوقيع (`auth_service.py`) | حرجة |
| G3 | الأمان | حد التوكنز مسجَّل لكن غير مفروض قبل استدعاء AI | عالية |
| G4 | الأمان | مفتاح أدمن افتراضي ضعيف + خطاف `sk_admin_` قديم + `.env` مرتكب | عالية |
| G5 | الجودة | صفر اختبارات آلية؛ لا ruff؛ CI يبتلع الأخطاء (`|| true`) | عالية |
| G6 | الجودة | كود ميت + لا ErrorBoundary + حالات تحميل/فارغة ناقصة | متوسطة |
| G7 | الجودة | ترقيات DB عبر try/except ALTER بلا تتبّع | متوسطة |
| G8 | التعاون | لا مشاركة/مساحات عمل مشتركة بين أعضاء الفريق | مطلوبة |
| G9 | الرندر | خلفيات `art` بألوان ثابتة لا تتبع الهوية؛ خطوط محدودة | متوسطة |
| G10 | الرندر | ترجمة/تلخيص تُقتطعان عند ~8000 حرف | متوسطة |
| G11 | التشغيل | لا Docker/nginx/نشر إنتاج؛ تحليلات ثابتة؛ اشتراك غير فعّال | متوسطة |
| G12 | متفرقات | 3 ملفات DB مكررة مُرتكبة؛ CORS عريض؛ rate-limit في الذاكرة | منخفضة |

---

## 3. تقسيم العمل: 3 مسارات

### المسار 1 — جودة الأكواد والبنية (Foundational: Quality & Engineering)
**الهدف**: توفير أرضية آمنة للعمل الجماعي (اختبارات، lint، صيانة).

**المهام والمؤشرات:**
- [ ] **T1.1** اختبارات الـ API: `pytest` + `httpx.TestClient` تغطي الراوتر كاملاً (`backend/routes/api.py`) — Auth، مستندات، رندر، قوالب، أدمن.
  - *ملفات العمل*: `backend/routes/api.py`, `backend/database.py`, جديد `backend/tests/`
  - *معيار القبول*: ≥ 80% تغطية لواجهات الحفظ/العرض/الحذف الرئيسية.
- [ ] **T1.2** اختبارات وحدة للخدمات: `ai_service` (output sanitization)، `rag_service`، `presentation_service` (normalize_deck/resolve_theme)، `pptx_theme_extractor`.
- [ ] **T1.3** اختبارات واجهات أمامية: إضافة `vitest` + `@testing-library/react` لأهمها (`PresentationView`, `TemplateModal`, `QuizView`).
- [ ] **T1.4** تفعيل `ruff` للبايثون (`pyproject.toml`/`ruff.toml`) في CI كخطوة معطلة على فشل.
- [ ] **T1.5** تشديد oxlint (تفعيل المزيد من القواعد) وجعل `npm run lint` يُفشل البناء في CI.
- [ ] **T1.6** تنظيف الكود الميت: `backend/services/pdf_service.py`, `frontend/src/components/AdminDashboardModal.jsx`, `DocumentLibraryModal.jsx`, `DocumentFAB.jsx`, `App.css` غير المستخدم.
- [ ] **T1.7** إضافة `ErrorBoundary` عام + حالات تحميل/فارغة ناقصة (Dashboard, Chat empty-state).
- [ ] **T1.8** نظام ترحيلات DB آمن (`schema_migrations` جدول) بدل try/except ALTER.
- [ ] **T1.9** تحسين CI: تشغيل الاختبارات فعلياً + `coverage` + شارة الحالة في README.

### المسار 2 — الأمان والحسابات (Security & Accounts)
**الهدف**: إغلاق الثغرات الحرجة وتأسيس هوية موثوقة للفريق.

**المهام والمؤشرات:**
- [ ] **T2.1** جلسات موقّعة: إنشاء `sessions` جدول + توليد توكن عشوائي عند الدخول، وقراءة المستخدم من الجلسة بدل `X-User-Id` المفتوح.
  - *ملفات العمل*: `backend/routes/api.py:69` (`_get_current_user`), `backend/database.py`, `frontend/src/services/api.js` (إرسال التوكن).
  - *معيار القبول*: إرسال `X-User-Id` مزوّر يرفضه الخادم (401).
- [ ] **T2.2** إغلاق تجاوز Google JWT: إزالة المسار الذي يثق بخلاصة الـ JWT بلا تحقق توقيع (`backend/services/auth_service.py:35-48`).
- [ ] **T2.3** إلغاء خطاف `sk_admin_` القديم (`database.py:309`) وإجبار `ADMIN_MASTER_KEY` من البيئة.
- [ ] **T2.4** كلمة مرور أدمن افتراضية: رفض/توليد عشوائي إن لم تُضبط من البيئة، وإزالة `backend/.env` من المستودع والاكتفاء بـ `.env.example`.
- [ ] **T2.5** فرض حد التوكنز: فحص `tokens_used >= tokens_limit` قبل أي استدعاء AI مع رسالة واضحة + نقاط استخدام مقدَّرة.
- [ ] **T2.6** إدارة مفاتيح API من الخادم: تخزين مفتاح المستخدم مشفّراً في DB، وإيقاف إرساله من المتصفح (إخفاء `X-Gemini-API-Key` من الشبكة).
- [ ] **T2.7** تحقق MIME + تعقيم اسم الملف عند الرفع (`routes/api.py:412-436`).
- [ ] **T2.8** rate-limit دائمياً (جدول DB أو ملف) بدل الذاكرة + احترامه عند عدة عمليات.
- [ ] **T2.9** التضييق: `allow_methods`/`allow_headers` محدّدة بدل `*` + مراجعة CORS.

### المسار 3 — التعاون والنشر والميزات (Collaboration, Deployment & Features)
**الهدف**: تمكين الزملاء من العمل على نفس المحتوى + حزمة نشر إنتاجية + إثراء العرض.

**المهام والمؤشرات:**
- [x] **T3.1** مساحة فريق: جدول `teams` + `team_members`، مشاركة مستند/عرض عبر عضوية الفريق، وكود دعوة قصير للانضمام.
  - *ملفات العمل*: `backend/database.py` (جداول/دوال), `backend/routes/api.py` (`/api/teams/*`), واجهة في `DashboardView`/`DocumentLibraryView`.
  - *معيار القبول*: زميلان كعضوين في فريق يريان نفس المستند ويشاهدان/يعدّلان نفس العرض مع فرض الأدوار.
- [x] **T3.2** تحسينات Deck Studio:
  - خلفيات `art` تصبغ بألوان هوية العرض الحالية (تطبيق `--art-tint` لكل شريحة).
  - أنماط تخطيطات مخصصة إضافية + دعم `fonts` أوسع مع خيار إضافة خطوط.
  - رفع حد الترجمة/التلخيص للوثائق الطويلة (التقديم المرحلي بدل الاقتطاع).
- [x] **T3.3** تحليلات حقيقية في اللوحة: استبدال الأرقام الثابتة بـ `GET /api/admin/stats` موثوق (مستندات/شرائح/دقائق/دينامية) + مقاييس مساحة الفرق (`total_teams`/`total_team_members`).
- [x] **T3.4** حزمة نشر: `Dockerfile` + `docker-compose.yml` (backend + frontend + reverse proxy nginx) وتوثيق نشر إنتاج في `README`.
- [x] **T3.5** توثيق التضمين: `frontend/README.md` مخصص، تحديث إجمالي الشجرة في `README.md`.
- [~] **T3.6** (مؤجل) اشتراكات: تكامل دفع فعلي + تجديد الحصة دورياً.

---

## 4. سير العمل المقترح معاً

- **فروع**: فرع مستقر `main` + فرع ميزة لكل عضو
  - `feat/quality` ← المسار 1
  - `feat/security` ← المسار 2
  - `feat/collab` ← المسار 3
- **حماية `main`**: Settings → Branches → Require PR + Require CI pass.
- **PR لكل مهمة**: فرع قصير العمر → وصف شامل → مراجعة زميل→ دمج squash.
- **مميزات الالتزامات**: Conventional Commits (`feat:`, `fix:`, `test:`, `refactor:`, `docs:`).
- **فك التعارض**: المسارات الثلاثة تكاد لا تلمس نفس الملفات:
  - المسار 1: `tests/`, `pyproject`, `*.test.jsx`, مكونات ميتة.
  - المسار 2: `routes/api.py` (auth/security), `database.py` (sessions), `auth_service.py`, `api.js` (headers).
  - المسار 3: جداول/دوال جديدة (`teams`), `presentation_engine` (art), `Dockerfile`, README.
  - > ملفات قد تتصادم: `routes/api.py` و`database.py` و`api.js` — التنسيق: مَن يلمسها يعلن في قناة الفريق أولاً، والمستهلك النسخ المطابق للسطر.

---

## 5. أولويات التنفيذ المقترحة (الأسابيع)

| الأسبوع | المسار 1 | المسار 2 | المسار 3 |
|---|---|---|---|
| 1 | pytest للـ API + ruff في CI | الجلسات الموقّعة (T2.1) + إغلاق Google JWT | جداول الفرق + وكود الدعوة (T3.1) |
| 2 | اختبارات الخدمات + vitest | فرض التوكنز + مفتاح الأدمن | مشاركة المستندات/العروض |
| 3 | تنظيف الكود الميت + ErrorBoundary | مفاتيح API من الخادم + MIME | Deck Studio تحسينات (art/خطوط) |
| 4 | ترحيلات DB + CI كاملة | rate-limit دائم + تحسين CORS | Docker + nginx + تحليلات |
| 5+ | استكمال التغطية والمراجعة | توثيق أمان + مراجعة شاملة | توثيق النشر والتضمين + اشتراكات |

---

*مسار كل مهمة يتبعه صاحب واحد لا غير؛ أي التباس تنسيق قبل البدء في قناة الفريق أو Issue في GitHub.*